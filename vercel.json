{
  "framework": "ember",
  "outputDirectory": "docs/dist",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm -F docs build",
  "git": {
    "deploymentEnabled": {
      "dist": false
    }
  }
}
